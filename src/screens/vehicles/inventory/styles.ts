import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  inputView: {
    marginTop: 24,
    marginBottom: 16,
  },
  radioButtons: {
    marginTop: 16,
    marginBottom: 16,
  },
});

export default styles;
