import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  mainContainer: {
    backgroundColor: "#fff",
  },
  container: {
    marginHorizontal: 18,
    marginTop: 35,
  },
});

export default styles;
