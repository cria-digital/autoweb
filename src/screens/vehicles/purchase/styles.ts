import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  inputView: {
    marginTop: 16,
    marginBottom: 10,
  },
  radioButtons: {
    marginTop: 12,
    marginBottom: 12,
  },
});

export default styles;
