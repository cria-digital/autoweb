import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  inputView: {
    marginTop: 24,
    marginBottom: 16,
  },
  radioButtons: {
    marginVertical: 28,
  },
});

export default styles;
